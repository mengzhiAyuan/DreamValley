<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mengzhiayuan.naruto.dao.DiscussPostMapper">

    <select id="selectDiscussPosts" resultType="DiscussPost">
        select id, user_id, title, content, type, status, create_time, comment_count, score
        from community.discuss_post
        where status!=2
        <if test="userId!=0">
            and user_id=#{userId}
        </if>
        <if test="orderMode==0">
            order by type desc,create_time desc
        </if>
        <if test="orderMode==1">
            order by type desc,score desc,create_time desc
        </if>
        limit #{offset},#{limit}
    </select>
    
    <select id="selectDiscussPostRows" resultType="int">
        select count(id)
        from community.discuss_post
        where status!=2
        <if test="userId!=0">
            and user_id=#{userId}
        </if>
    </select>

    <insert id="insertDiscussPost" parameterType="DiscussPost" keyProperty="id">
        insert into community.discuss_post
            (user_id, title, content, type, status, create_time, comment_count, score)
        values(#{userId},#{title},#{content},#{type},#{status},#{createTime},#{commentCount},#{score})
    </insert>

    <select id="selectDiscussPostById" resultType="DiscussPost">
        select id, user_id, title, content, type, status, create_time, comment_count, score
        from community.discuss_post
        where id=#{id}
    </select>

    <update id="updateCommentCount">
        update community.discuss_post set comment_count= #{commentCount} where id= #{id}
    </update>

    <update id="updateType">
        update community.discuss_post set type = #{type} where id = #{id}
    </update>

    <update id="updateStatus">
        update community.discuss_post set status = #{status} where id = #{id}
    </update>

    <update id="updateScore">
        update community.discuss_post set score = #{score} where id = #{id}
    </update>

</mapper>